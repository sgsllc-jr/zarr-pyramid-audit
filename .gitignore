# virtualenv
.venv/
venv/

# python
__pycache__/
*.py[cod]
*.egg-info/

# timestamped backups created by our own never-overwrite policy
*.bak
*.[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9]-[0-9][0-9][0-9][0-9][0-9][0-9].bak

# scratch / intermediate run output.
# Published evidence lives in artifacts/, copied there deliberately.
tmp/

# Working drafts held back from publication until their numbers are corrected.
# (The 2026-09-08 draft still carries the pre-count_chunks storage estimate that
#  the measured run superseded; publishing it would ship a known-false figure.)
reports/*DRAFT*
